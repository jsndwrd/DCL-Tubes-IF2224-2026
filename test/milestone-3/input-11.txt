program ComplexExpr;

var
  a, b, c: integer;
  r: real;
  flag: boolean;

begin
  a := 1;
  b := 2;
  c := 3;
  r := (a + b) * 2.5;
  flag := (a < b) and (b < c) or (a == c);
  flag := not flag;
  writeln('done');
end.
